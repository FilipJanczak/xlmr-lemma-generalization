# Direct dependencies of main.ipynb and download_ud.py, pinned to the versions that produced the
# thesis results (thesis Table C.2.1). requirements.txt locks the complete environment built from
# this list; edit this file, not that one, when a dependency changes.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
transformers==5.14.1
tokenizers==0.22.2  # the fast XLM-R tokenizer
huggingface_hub==1.24.0
scikit-learn==1.9.0
numpy==2.4.6
scipy==1.17.1
statsmodels==0.14.6
pandas==3.0.3
pyarrow==25.0.0  # parquet engine for the caches
matplotlib==3.11.1
conllu==6.0.0
requests==2.34.2  # download_ud.py
ipykernel==7.3.0  # to run the notebook
